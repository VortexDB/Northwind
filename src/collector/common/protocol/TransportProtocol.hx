package collector.common.protocol;

import collector.common.channel.TransportChannel;
/**
 * Protocol for transporting data to device
 */
class TransportProtocol {
    /**
     * Some channel to transfer data
     */
    public var channel:TransportChannel;
}